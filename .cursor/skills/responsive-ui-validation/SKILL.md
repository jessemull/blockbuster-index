---
name: responsive-ui-validation
description: >-
  Validate responsive layout using Tailwind breakpoints and useBreakpoint.
---

# Responsive UI Validation

Read `docs/RESPONSIVENESS.md`.

Verify `sm`/`md`/`lg` behavior for charts, badges, rankings columns, and chat sheet. Prefer Tailwind; use `useBreakpoint` only for JS-driven chart options.
