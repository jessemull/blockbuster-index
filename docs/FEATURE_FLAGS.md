# Feature Flags

> **Status:** No feature-flag system is in use today.

---

## If adding later

- Prefer simple env-based `NEXT_PUBLIC_*` toggles for static export (inlined at build time).
- Document flag name, default, and cleanup plan in the PR.
- Do not add a heavyweight flag service without product approval.
